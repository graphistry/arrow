import { FlatListBuilder } from './base';
export class BinaryBuilder extends FlatListBuilder {
    writeValue(value, index = this.length) {
        return super.writeValue(value, index);
    }
}

//# sourceMappingURL=binary.mjs.map
